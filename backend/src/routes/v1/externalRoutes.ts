/**
 * @summary
 * External (public) API routes configuration.
 * Handles public endpoints without authentication.
 *
 * @module routes/v1/externalRoutes
 */

import { Router } from 'express';

const router = Router();

export default router;
